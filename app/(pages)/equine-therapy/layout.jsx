export const generateMetadata = () => {
    return {
        title: 'Equine Therapy'
    }
}

export default function Layout({ children }) {
    return (
        <>
        {children}
        </>
    );
}