export const generateMetadata = () => {
    return {
        title: 'Counseling'
    }
}

export default function Layout({ children }) {
    return (
        <>
        {children}
        </>
    );
}